// Date helpers for article timestamps.
// Implementation comes in a later step.

export function formatDate(value) {
  // TODO: return a readable absolute date.
  return value ?? '';
}

export function formatRelativeTime(value) {
  // TODO: return "3 hours ago" style output.
  return value ?? '';
}
